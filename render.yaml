services:
  - type: web
    name: price-tracker
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: "production"
      - key: DATABASE_URL
        value: "file:./data/price_tracker.db"
